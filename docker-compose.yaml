version: "3"

services:
  telegram-bot:
    build: .
    restart: always
    command: cron && tail -f /var/log/cron.log
    volumes:
      - ./logs:/code/logs
      - ./results:/code/results
    environment:
      - TZ=Europe/Moscow 